---
description: 项目通用规范（命名、可维护性、变更策略）
alwaysApply: true
---

# 项目通用规范

- 输出与文档优先使用简体中文；仅在必要时使用英文术语。
- 做“可回滚的小步提交”：一次变更聚焦一个目的，避免混杂重构 + 新功能。
- **稳定对外契约**：REST API 字段命名尽量稳定；破坏性变更需要版本化（例如 `/api/v2`）。
- **版本化持久化**：localStorage key 需要带版本（例如 `xxx.v1`）；升级时提供迁移或兼容读取。
- 不提交敏感信息（生产库连接、密钥等）。

